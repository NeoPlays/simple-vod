# Server
PORT=8080

# Paths (defaults work out of the box in Docker; change only if needed)
DB_PATH=/data/db/sqlite.db
VIDEO_DIR=/data/videos
FRONTEND_DIR=/app/frontend

# Security — MUST be changed before going to production
PASSWORD_PEPPER=change-me-in-production
